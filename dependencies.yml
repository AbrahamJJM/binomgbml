name: binomgbml
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python >= 3.8
  - pytorch >= 2.0
  - pytorch-cuda >= 11.7
  - torchvision
  - pip
  - learn2learn >= 0.2.0
  - numpy >= 1.21.0
